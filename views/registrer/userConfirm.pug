extends ../layout.pug

block metadata
    title Confirmacion del usuario

block content
    body(class="flex flex-col justify-center items-center absolute left-0 right-0 top-70")
        div(class="flex flex-col bg-green-300 text-[25px] font-bold text-green-900 p-10 items-center")
            p=msg ? msg : "Usuario creado exitosamente" 
            p(class="mt-3") [✓] Paso 2 de 2: Creacion del perfil
            a(href="/" class="hover:text-blue-400 text-black mt-10") 
                | Haz click aqui para volver al Home